
import { PageConfig } from '../../common/page-config';


const homePageConfig: PageConfig = {
  title: 'Website Starter Boilerplate',
  templatePath: `${__dirname}/home.twig`,
  filename: 'index.html',
  chunks: ['index'],
};

export default homePageConfig;
